import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  margin-top: 1em;
  margin-left: 6em;
  margin-right: 6em;
`;
export const Settings = () => (
  <Wrapper>
    <h2>Settings page (TO DO)</h2>
  </Wrapper>
)